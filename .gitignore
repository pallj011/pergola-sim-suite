# --- macOS filesystem metadata ---
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
.DocumentRevisions-V100
.TemporaryItems
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
Icon?

# --- Helm ---
# Packaged charts and fetched subchart dependencies (not the chart sources)
*.tgz
charts/*/charts/
charts/*/Chart.lock
.cr-release-packages/
.cr-index/

# --- Editor / OS noise ---
*.swp
*.swo
*~
.idea/
.vscode/
*.bak
*.orig
*.tmp

# --- Local overrides (may contain secrets / license servers) ---
values.local.yaml
*-local-values.yaml
secrets.yaml

# --- Claude Code local settings (machine-specific permissions) ---
.claude/settings.local.json
